from pygls.lsp.types.basic_structures import Position
from pygls.lsp.types.basic_structures import Range

selected_range = Range(
    start=Position(line=6, character=4),
    end=Position(line=6, character=4),
)
selected_code = "c"
